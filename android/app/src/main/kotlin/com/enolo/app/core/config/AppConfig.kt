package com.enolo.app.core.config

import com.enolo.app.BuildConfig

object AppConfig {
    /** Default server URL placeholder. Edit BuildConfig.DEFAULT_SERVER_URL before building. */
    val defaultServerUrl: String = BuildConfig.DEFAULT_SERVER_URL
}
